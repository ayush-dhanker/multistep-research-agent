# multistep-research-agent
Multi-step research agent that plans, fetches from parallel sources, and iteratively refines output until quality threshold is met, it is built with LangGraph, featuring sequential, parallel, conditional, and iterative workflow patterns.
