export default function Loader() {
    return (
        <div className="loader">
            <div className="spinner" />
            <p>Agent is thinking — planning, fetching, evaluating...</p>
        </div>
    )
}